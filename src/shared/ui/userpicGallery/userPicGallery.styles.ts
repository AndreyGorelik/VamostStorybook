import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  row: {
    flexDirection: 'row',
  },
  userpic: {
    borderRadius: 100,
    borderWidth: 3,
    borderColor: 'white',
  },
});
