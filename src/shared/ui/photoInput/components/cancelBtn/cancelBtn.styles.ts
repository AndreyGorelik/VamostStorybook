import { StyleSheet } from 'react-native';

export const createStyles = () =>
  StyleSheet.create({
    wrapper: {
      position: 'absolute',
      right: 5,
      top: 5,
      zIndex: 10,
    },
  });
