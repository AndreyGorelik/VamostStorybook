import PostCreate from './postCreate.component';

export default {
  title: 'PostCreate',
  component: PostCreate,
};

const Template = () => {
  return <PostCreate />;
};

export const Default = Template.bind({});
