import { StyleSheet } from 'react-native';

export const createStyles = () =>
  StyleSheet.create({
    wrapper: {
      paddingTop: 10,
      paddingBottom: 80,
    },
    separator: {
      height: 20,
    },
  });
